const H = require("./helpers.js");
const { d, p, t, mono, h, bullet, numbered, table, callout } = H;
const { code } = require("./part3.js");

function evidence(title, lines) {
  return callout(title, lines, { bg: "EAF1F8", color: H.ACCENT });
}

module.exports = [
  h("5.9 Monte Carlo protocol and sampling", 2),
  p("One episode = 1 claim × 1 topology × 1 condition × 1 seed. Claims and topologies randomised across episodes; conditions assigned by grid. Claims stratified by LIAR severity (pants-fire / false / barely-true) and topic; placebo claims matched on topic and length. Decoding fixed across conditions (citizens 0.7, judges 0.2); every episode seed is derived from a stable digest over its inputs — reproducible across processes and platforms rather than from a per-process-salted hash — and is pinned by a regression test. Every episode is written as JSONL and aggregated into versioned Parquet. Payoff accounting is unit-tested against hand-computed episodes before any grid run — a silent ledger bug would invalidate every primary outcome."),
  p([t("Sampling is two-tier by design. ", { bold: true }),
     t("The core grid runs 30 seeds/cell, which is enough to power the intervention-vs-control contrast and the model-dependence contrast (H5); spending uniformly across every cell to buy power for the contrasts that need far more (H1 at ~105 seeds/cell, H3 at ~572) would cost several times the core grid to reach cells it does not need to. Those contrasts are declared exploratory before the grid runs (§4, §8), and one high-power confirmatory rerun at 200 seeds/cell is carried in the budget below for the contrast most worth buying outright, rather than inflating every cell uniformly.")]),
  table([30, 11, 11, 13, 35], [
    ["Arm", "Cells", "Seeds", "Episodes", "What it powers"],
    ["Core grid (2×2×L + L controls, L = 4 here)", "20", "30", "600", "Intervention vs control on FPR and EPC; H5 model dependence; all descriptive cell means"],
    ["Placebo arm (specificity)", "20", "10", "200", "RQ5, pooled and conditional on the intervener having fired"],
    ["Ablation 1 — payoff visibility", "20", "3", "60", "Whether the game structure drives behaviour at all"],
    ["Ablation 2 — incentive ratio β/γ ∈ {0.5, 1, 3}", "60", "3", "180", "Platform-design sensitivity"],
    ["Scale check (N = 50)", "4", "10", "40", "Direction agreement between N = 7 and N = 50"],
    ["Perturbation arm", "40", "8", "320", "Norm robustness: one citizen defects at the half-way round, either amplifying the claim or refusing to pay the correction cost. Follows GovSim §3.3."],
    ["Mixed-society arm", "—", "150 replications, seats rotated", "150", "H6 / RQ6: whole roster seated in one episode, seat assignment rotated by replication so every model holds every seat equally often. Same call count as a monoculture episode — cost-neutral. See §5.11."],
    [{ text: "Total", bold: true }, { text: "—", bold: true }, { text: "—", bold: true },
     { text: "1,550", bold: true }, { text: "The grid the reference implementation actually runs, at L = 4 backbone levels", bold: true }],
  ]),
  p([t("Episode counts scale with the number of backbone levels. ", { bold: true }),
     t("Every arm except the scale check and the mixed-society arm is indexed by backbone, so a design with L levels has 2×2×L + L cells: 20 at the L = 4 the reference implementation runs, and 30 at the L = 6 live roster of §7. Carried through every arm, the live grid is 2,230 episodes rather than 1,550 — 900 core, 300 placebo, 90 and 270 for the two ablations, 480 perturbation, plus the 40 scale-check and 150 mixed-society episodes, which do not scale. The marginal cost of one more level is the fixed quantity to plan against, and it is measured rather than assumed: 23,250 calls per level (§5.9).")]),
  evidence("Feasibility evidence: the budget is metered, not estimated", [
    "The framework meters the prompts the episode loop actually assembles and prices them against the published rate card; the command is reproducible (`nudgesim cost`) and the numbers below come from a run against the real LIAR and PHEME-9 corpora.",
    [t("What a call costs. ", { bold: true }), t("60 model calls an episode — five citizens over twelve rounds; the disseminator and the intervener run on fixed policies and cost nothing. 613 input tokens per call on average, 877 at the longest. 23,250 calls per model over the 1,550-episode grid, because factor F3 gives each model its own replications of every cell: the grid grows linearly in models benchmarked, and that, not prompt length, is what a multi-model design costs.")],
    [t("The lever that does not exist. ", { bold: true }), t("Across every metered call, not one prompt recurred — each carries a distinct claim, a distinct agent and a distinct feed history, so a response cache never serves a request. Roughly half of each prompt (50.1%) is the persona prefix, which is identical across an agent's twelve turns — prompt caching is the real lever, and batch endpoints roughly halve cost again.")],
    [t("Where the money actually goes. ", { bold: true }), t("Reasoning tokens bill as output. One frontier arm with extended thinking costs on the order of $1,270 a grid against roughly $110 without it — and the reasoning contrast is one of the most informative arms to run (§7). Scheduling that arm to run once on the main grid and once more at high power, rather than in every calibration, validation and bug-fix rerun, is the single decision that keeps the programme affordable: recommended four-arm portfolio, roughly $2,050 total, self-funded, requiring no lab funding.")],
  ]),

  h("5.10 Communication channel", 2),
  p("The two reference designs this work follows answer the communication question in opposite directions, and each answer determines what the study can identify."),
  table([22, 39, 39], [
    ["", "SanctSim", "GovSim"],
    ["Channel", "None. Agents interact solely through their game decisions.", "Free-form group chat before each decision round."],
    ["Consequence", "Every behavioural difference is attributable to the payoff structure, because nothing else passes between agents.", "Communication is the largest single lever in the paper: negotiation is 62% of dialogue, and removing the chat raises resource over-use by 21 points."],
  ]),
  p([t("NudgeSim splits the two by channel. ", { bold: true }),
     t("The decision channel is SanctSim-style: a citizen chooses from a fixed four-way action space, and nothing a citizen writes changes another citizen's payoff except through the action it accompanies. The payoff ledger is a function of actions alone, which is what keeps the identification clean. The observable channel is GovSim-style: each action carries the agent's own words, and those words are what neighbours see.")]),
  p([t("The second half is forced by the subject matter rather than chosen. ", { bold: true }),
     t("Intervention tone is a manipulated factor, and tone has no carrier other than natural language. A design without an observable language channel deletes factor F2, and with it H3 — the headline hypothesis. There is no version of this study that runs on decisions alone.")]),
  p([t("What the split buys. ", { bold: true }),
     t("Because language rides on each action rather than flowing in a free group chat, every utterance is attached to the decision it justified and to the payoff that decision incurred. That is what makes the reasoning-trace taxonomy (§5.6) and the reactance measure well-defined per decision rather than per conversation.")]),
  p([t("What it costs, stated as a limit. ", { bold: true }),
     t("NudgeSim cannot observe negotiation or coalition formation, which is GovSim's most interesting result. Copying GovSim's group chat outright would make the intervener's tone one voice among many and let citizens coordinate a joint response to it — so the tone contrast would then estimate \"aggressive tone, plus whatever the group negotiated in response to it\", which is not the estimand. The deliberation channel belongs in the follow-on design (§13), where citizens vote on whether a corrector should exist at all; there, coordination is the object of study rather than a confound.")]),

  h("5.11 Mixed-society arm (composition)", 2),
  p("Every study in this line that varies the backbone runs monocultures: one model per simulation, varied between simulations. That design answers whether model choice moves the aggregate (H5). It cannot answer whether one model free-rides on another's contribution, because in a society of identical agents there is no other model to free-ride on. Two of the four reference studies already add a heterogeneous experiment as a headline question rather than an appendix — MoralSim pairs each model against every other in an opponent matrix, and CoopEval runs replicator dynamics over an initially uniform mixture."),
  p("The mechanisms NudgeSim measures make this unavoidable rather than merely desirable. Conformity to a local majority, and declining to pay a cost somebody else is already paying, are both claims about what one agent does in response to another. Under a monoculture those two agents are near copies, so a monoculture-only design cannot separate \"this model corrects\" from \"this model corrects when surrounded by itself\"."),
  p([t("Design. ", { bold: true }), t("The mixed arm seats the whole roster in one society. Which model holds which seat rotates by replication, so every model occupies every seat equally often — enforced by a test, not left to chance — because seats differ in network position and a fixed assignment would confound model with position permanently: whatever sat in the best-connected seat would read as a property of that model. Two contrasts follow that are not available from the core (monoculture) grid: what each model does inside the same episode with claim, topology and intervention held exactly fixed; and the same model's correction rate among other models against its rate among copies of itself.")]),
  evidence("Feasibility evidence: what the mixed arm already shows", [
    "Measured on the reference implementation's analytic surrogate policy, against real LIAR and PHEME-9 — a proxy for what the mixed arm will measure once real backbones are seated:",
    [t("Backbone effects survive perfect control. ", { bold: true }), t("H5 measured within an episode rather than between: nothing differs between seated policies except which policy sits in the seat — not the claim, not the topology, not the intervention, not the seed — and the correction rate still spans a factor of 5.4 (EPC 0.023 to 0.121). A between-episode comparison cannot rule out that part of such a gap is claim or topology draw; this one can.")],
    [t("Whether effort depends on who the neighbours are: a null, so far. ", { bold: true }), t("No policy's correction rate detectably depends on whether its neighbours are copies of itself — the largest gap is −0.034 [−0.061, −0.006], and nothing survives Holm correction. This is informative about the surrogate rather than a disappointment: an analytic policy responds to the count of visible correction, not to who is doing it, so there is no channel by which a neighbour's identity could matter. That is precisely the assumption a language model need not satisfy, which is why this arm is worth running on real backbones.")],
    [t("Cost. ", { bold: true }), t("Zero additional spend. A mixed episode issues the same number of model calls as a monoculture episode — five citizens over twelve rounds either way — only the calls are split across backbones instead of going to one. The 150 replications above are already inside the 1,550-episode total in §5.9, not an addition to the budget.")],
  ]),

  h("6. Data Resources", 1),
  table([18, 36, 28, 18], [
    ["Resource", "Role in study", "Key properties", "Access"],
    ["LIAR (Wang, 2017)", "Claim pool: false-side statements as injected misinformation; matched true statements as placebo controls.", "12.8K short PolitiFact statements, six-way veracity labels, speaker and context metadata.", "Loaded from the original UCSB archive; the Hugging Face loading script is deprecated."],
    ["PHEME-9 (Kochkina, Liakata & Zubiaga, 2018)", "Topology source and calibration target: reply trees initialise adjacency; cascade statistics validate the control baseline.", "Twitter rumour threads across nine breaking-news events; per-thread reply trees and veracity annotations.", "figshare DOI 10.6084/m9.figshare.6392078; annotations CC-BY."],
  ]),
  p([t("Preprocessing safeguards. ", { bold: true }),
     t("Real speaker names in LIAR statements are replaced with generic attributions in all agent-visible text, so the simulation never generates novel false associations about identifiable individuals — organisation-shaped speakers (PACs, newspapers, \"the big Wall Street banks\") are filtered out of the surname pass rather than mangled by it. From PHEME we redistribute derived topology statistics only, never tweet content. No new human data is collected; the human coding in §5.6 is performed by the research team on model-generated text.")]),
  evidence("Feasibility evidence: de-identification is a tested property", [
    "Both failure directions are enforced by tests rather than left as an intention, because both are silent: leaking a real name into agent-visible text, or over-redacting an ordinary noun phrase into nonsense that would degrade every behavioural measure without ever raising an error. On the reference implementation, residual name leakage is 0.00% with 11% of speaker-column tokens replaced, and ordinary noun phrases are left intact.",
  ]),

  h("7. Codebase and Technical Stack", 1),
  p([t("Primary engine. ", { bold: true }),
     t("A purpose-built, lightweight asyncio orchestration loop, rather than a fork of OASIS for the core experiment: OASIS is engineered for populations from hundreds to a million agents, and running it at N = 7 pays its integration overhead while using none of its strengths. The core study instead needs something OASIS does not provide out of the box — a payoff ledger with per-round accounting, cost debiting, and end-of-episode veracity resolution. Building that directly is both faster and easier to unit-test.")]),
  p("OASIS is retained where it is actually the right tool: the N = 50 scale check (§5.8) runs on OASIS with an identical logging schema, so the two phases produce interoperable data. This is a deliberate right-tool-for-the-job split rather than a fallback."),
  p([t("Model roster. ", { bold: true }),
     t("Chosen cross-vendor and for overlap with the GovSim / SanctSim / MoralSim / CoopEval line, so that a model-dependence result here can be read directly against theirs rather than sitting on a roster no prior study shares.")]),
  table([20, 8, 72], [
    ["Study", "n", "Models"],
    ["GovSim (2024)", "15", "GPT-3.5, GPT-4, GPT-4-turbo, GPT-4o; Claude-3 Haiku / Sonnet / Opus; Llama-2 7B/13B/70B, Llama-3 8B/70B; Mistral-7B, Mixtral-8x7B; Qwen-72B/110B"],
    ["SanctSim (2025)", "9", "DeepSeek-V3, GPT-4o, GPT-4o-mini, Llama-3.3-70B; o1-mini, o1-preview, o3-mini (low / med / high)"],
    ["MoralSim (2025)", "9", "DeepSeek-R1, o3-mini, Qwen-3-235B-A22B; Claude-3.7-Sonnet, DeepSeek-V3, Gemini-2.5-Flash, GPT-4o-mini, GPT-4o, Llama-3.3-70B"],
    ["CoopEval (2026)", "6", "Claude Sonnet 4.5, GPT-5.2, Gemini 3 Flash (reasoning and base), GPT-4o, Qwen3-30B-A3B"],
  ]),
  p([t("GPT-4o is the only model in all four, which makes it the comparability anchor. Open-weights models appear in all four. Anthropic appears in three of the four. The roster below follows that shape.")]),
  table([24, 16, 60], [
    ["F3 level", "Vendor", "Why this level is in the design"],
    ["GPT-4o", "OpenAI", "The anchor. Present in all four reference studies, so a model-dependence result here can be read directly against theirs. Non-reasoning."],
    ["A current OpenAI reasoning model", "OpenAI", "The reasoning contrast held within one vendor, which is the comparison SanctSim drew (o-series against the traditional four) and the one its headline rests on."],
    ["claude-opus-5, thinking off", "Anthropic", "Frontier non-reasoning, and the paired control for the row below."],
    ["claude-opus-5, thinking on", "Anthropic", "The reasoning contrast held at fixed weights. CoopEval does this with Gemini 3 Flash; MoralSim notes Claude-3.7-Sonnet and Gemini-2.5-Flash offer both modes and takes the non-reasoning variant for cost. Running it on a frontier model is the extension, and §12 is why it is worth the spend."],
    ["Llama-3.3-70B", "Meta (open)", "In SanctSim and MoralSim. Served locally through the same OpenAI-compatible interface, so it costs GPU time rather than tokens."],
    ["Qwen3-30B-A3B", "Alibaba (open)", "In CoopEval and (at 235B) MoralSim. The bottom of the capability ladder, and the arm that shows a result was not bought with spend — SanctSim's GPT-4o-mini out-contributed GPT-4o, so this is not a formality."],
  ]),
  p([t("Six levels give 2,230 episodes, roughly 370 per level. ", { bold: true }),
     t("Each level is charged its own replications of every backbone-indexed cell, so the marginal cost of one more model is fixed and vendor-independent — on the order of 22,000 model calls, metered at 23,250 for the L = 4 reference grid (§5.9), where the episode-count scaling is set out. The Anthropic levels are priced there; the other four rate cards move independently and should be priced at the time of the run rather than quoted here.")]),
  p([t("What this needs that the reference implementation does not have. ", { bold: true }),
     t("Credentials for OpenAI, and either a GPU to serve the two open-weights models or an inference host for them. No new code: every hosted model in the table above is reachable through the one OpenAI-compatible backend already in the engine, and open weights through the same interface pointed at a local vLLM server. This is an access dependency, not an engineering one, and it is the critical path for the whole benchmark.")]),

  p([t("Judging. ", { bold: true }),
     t("A separate, stronger judge model — distinct from every citizen backbone — for trace classification and manipulation checks, so that no model grades its own output. Primary outcomes never pass through a judge; they are read off the action log.")]),
  p([t("Stack. ", { bold: true }),
     t("Python 3.11+ · Hugging Face datasets · NetworkX (topology) · pandas / Parquet (logs) · statsmodels and lifelines (analysis) · Matplotlib (figures) · YAML configs with pinned seeds.")]),
  p([t("Status. ", { bold: true }),
     t("A reference implementation of this layout exists, passes 144 tests including hand-computed payoff-ledger checks, and reproduces the full 1,550-episode grid with one command — including a pilot run against the real LIAR and PHEME-9 corpora. Nothing in the game, metrics or analysis layers depends on which model — or whether any model — supplies the citizen policies, so swapping the six backbones in is a configuration change.")]),
  code([
    "nudgesim/",
    "├─ configs/          # experiment grid, seeds, payoff parameters (YAML)",
    "├─ data/",
    "│   ├─ liar/         # claim-pool builder (severity/topic stratification, de-ID)",
    "│   └─ pheme/        # reply-tree parsing → 7-node motifs + cascade statistics",
    "├─ src/",
    "│   ├─ game/         # action space, payoff ledger, equilibrium reference (UNIT-TESTED)",
    "│   ├─ agents/       # persona suites (A1/A2, B1/B2, C, disseminator, intervener)",
    "│   ├─ backends/     # provider-agnostic LLM interface + response cache",
    "│   ├─ env/          # asyncio episode loop + termination guards",
    "│   ├─ oasis/        # N=50 scale-check adapter, shared logging schema",
    "│   ├─ intervention/ # timing × tone scheduler, payload templating",
    "│   └─ metrics/      # action-log metrics, trace classifier, probes",
    "├─ analysis/         # preregistered models, survival analysis, power, figures",
    "└─ runs/             # JSONL episode logs → versioned Parquet",
  ]),

  h("8. Analysis Plan", 1),
  p([t("Primary. ", { bold: true }),
     t("Mixed-effects models on episode-level cumulative FPR and EPC, with fixed effects for timing × tone × backbone, random intercepts for claim, topology and seed block, and intervener reach as a covariate (§5.2). Dunnett contrasts of each treatment cell against its own-model control — pooling controls across backbones would confound the intervention effect with backbone differences, which H5 says may be the larger of the two.")]),
  p([t("Headline test (H3). ", { bold: true }),
     t("Sign and significance of the tone effect on EPC relative to control, tested separately from the tone effect on FPR. A dissociation — aggressive tone lowering FPR while also lowering EPC — is the paper's central claim if it holds; §4 explains why this contrast is exploratory rather than confirmatory at the core grid's sample size.")]),
  p([t("Specificity (RQ5). ", { bold: true }),
     t("Two preregistered contrasts, not one: the pooled treated-versus-control comparison, and the same comparison conditional on the intervener having actually fired. Pooling alone averages the intervener's false alarms together with the episodes where it correctly stayed silent, and those carry opposite information.")]),
  p([t("Model dependence (H5) and composition (H6). ", { bold: true }),
     t("Variance decomposition comparing between-model and between-condition effect sizes, plus a three-way interaction test, on the core grid; the same contrast measured within a single mixed episode (§5.11), and a second contrast asking whether a model's correction rate depends on whether its neighbours are copies of itself.")]),
  p([t("Durability. ", { bold: true }),
     t("Area under the post-intervention FPR curve as the primary measure; Kaplan–Meier curves and log-rank tests on the relative-threshold half-life as secondary (§5.5).")]),
  p([t("Effect sizes in outcome units. ", { bold: true }),
     t("A mixed model says whether a factor matters once everything else is held fixed; it does not say by how much, in the units the outcome is measured in. Following MoralSim, every manipulated factor also carries an average treatment effect with a bootstrap 95% CI, marginalised over the other factors and repeated within each backbone — a design effect that reverses sign across models is a different finding from one that does not, and a pooled coefficient hides that.")]),
  p([t("A human row. ", { bold: true }),
     t("Model numbers have no scale on them unless something human sits beside them. Following SanctSim's Table 1, the results table carries a row from Gürerk, Irlenbusch & Rockenbach (2006) on two quantities both designs define: take-up of costly enforcement, and sanctioning relative to affirmation. Absolute levels are not comparable between a token-contribution game and a misinformation game, so each entry declares that it licenses an ordering and nothing stronger.")]),
  p([t("Norm robustness. ", { bold: true }),
     t("Following GovSim §3.3, a defector is seated among the citizens at the half-way round — either an amplifier that backs the false claim every round, or a free rider that stops paying the correction cost — and the question is what the other citizens do about it. Outcomes exclude the perturbation seat in both arms, so a fall means peers stood down rather than that the defector's own silence was counted.")]),
  p([t("Inference hygiene. ", { bold: true }),
     t("Bootstrap 95% CIs, Holm correction within each outcome family, effect sizes (partial η², Cohen's d, odds ratios) reported alongside p-values.")]),
  p([t("Robustness. ", { bold: true }),
     t("Payoff-visibility ablation, β/γ sweep, topology type, intervener reach, claim severity, decoding temperature, judge swap, and human-validated κ for every judge-dependent measure.")]),
  p([t("Power. ", { bold: true }),
     t("The analysis plan carries an explicit power statement for every primary contrast, computed from the Month-2 pilot and frozen with the preregistration. A contrast whose 80%-power requirement exceeds its allocated sample is declared exploratory before the grid runs, not reinterpreted afterwards. The pilot's power analysis is itself a deliverable of the Week-8 gate.")]),
  p([t("Validation of the analysis itself. ", { bold: true }),
     t("Before the analysis is trusted to report an effect, it is run against three declared data-generating processes: the effect present at its declared size, inflated, and set to zero. It must find the effect, find more of it, and find none of it, in that order — the gate that would catch, for instance, a seeding defect that let two nominally identical runs silently disagree.")]),
  p([t("Preregistration. ", { bold: true }),
     t("The full analysis plan, including the primary/secondary split, the confirmatory/exploratory tiering, the power statement and all stopping rules, is frozen on OSF at the end of Month 2, before any main-grid run.")]),
];
